# Reviewer 1 Remaining-Item Contract

This Overleaf-facing note records the compact contract for the already
completed remaining Reviewer 1 items: R1-M2 through R1-M5 and R1-m1 through
R1-m9. The detailed workflow-side contract is tracked at
`docs/reviewer1_remaining_contracts_20260615.md` in the workflow repository.

The protected manuscript behavior is:

- one common state-space formulation is linked to the forecast-validation rows
  through likelihood, source-set, and transfer-treatment labels;
- PIT, quantile-crossing, and implementation-level algorithm details do not
  drive the main narrative;
- forecast evidence is the five-cutoff rolling-origin evaluation with held-out
  USGS verification, not a continuous post-2022 hindcast;
- fair assessment is described as a forecast-origin, time-ordered validation
  design;
- data-role, latest-forecast, precipitation-zero, figure-interpretation, and
  posterior-table center policies remain consistent with the response letter.

The workflow validators check this note, the main manuscript, generated table
TeX, and the corrections response together. Any future manuscript refresh should
therefore be followed by the cross-repo validation pass before the article is
repulled into Overleaf.
