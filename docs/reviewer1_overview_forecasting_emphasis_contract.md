# Reviewer 1 Overview Forecasting-Emphasis Contract

This note documents the manuscript-facing contract for the Reviewer 1 overview
response.

The revised article treats rolling-origin forecast validation as the main
empirical evidence. The introduction states that the empirical focus is
forecasting performance and uncertainty quantification, the results section is
organized around out-of-sample forecast validation, and the selected-model
dynamics and synthesis figures are presented as interpretation and illustration
rather than as additional validation evidence.

The corresponding workflow validator is
`scripts/reviewer1_overview_contract.py` in the workflow repository. It checks
that the revised article and corrections response keep this structure and do not
reintroduce stale main-article framing such as `General Results` or staged
`Model A/B/C` language.
